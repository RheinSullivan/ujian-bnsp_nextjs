export const metadata = {
  title: "Data Peserta Sertifikasi",
};

const PesertaLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="p-10">{children}</div>;
};

export default PesertaLayout;
